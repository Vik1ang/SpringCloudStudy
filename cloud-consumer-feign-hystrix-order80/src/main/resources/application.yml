server:
  port: 80

ribbon:
  ReadTimeout: 4000
  ConnectTimeout: 4000

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

feign:
  hystrix:
    enabled: true